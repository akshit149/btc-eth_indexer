# RPC Configuration (Required)
# Replace with your actual RPC URLs
BTC_RPC_URL=http://user:pass@localhost:8332
ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_KEY

# Database Configuration (Defaults for Docker)
DB_HOST=postgres
DB_PORT=5432
DB_USER=indexer
DB_PASSWORD=indexer
DB_NAME=indexer

# Redis Configuration (Defaults for Docker)
REDIS_ADDR=redis:6379

# API Configuration
SERVER_PORT=8080
AUTH_RATELIMIT_REQUESTS=1000

# Frontend Configuration
NEXT_PUBLIC_API_URL=/api
